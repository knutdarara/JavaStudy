소스트리 + 버튼 눌러서
clone 버튼 누르고
(폴더가 비어 있어야 충돌이 안남)

main 브랜치 pull 로 땡겨옴.

파일 변경시에 커밋으로 파일 올리고
푸쉬 눌러서 자기 브랜치만 선택해서
홈페이지 에서 풀 리퀘스트 할것!

queue, stack

git init
git add .
git commit -m “commit message”
git remote add origin https://github.com/knutdarara/JavaStudy.git
git push -u origin master